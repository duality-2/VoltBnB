{
  "rules": {
    ".read": false,
    ".write": false,
    "sessions": {
      "$bookingId": {
        ".read": "auth != null && (data.child('hostUid').val() === auth.uid || data.child('renterUid').val() === auth.uid)",
        ".write": "auth != null && ((!data.exists() && newData.child('hostUid').val() === auth.uid && newData.child('renterUid').exists()) || (data.exists() && data.child('hostUid').val() === auth.uid && newData.child('hostUid').val() === data.child('hostUid').val() && newData.child('renterUid').val() === data.child('renterUid').val()))"
      }
    }
  }
}